simulation:
  num_rounds: 1
  turns_per_phase: 3
agents:
  names: [Kate, Jack]
  starting_resources: 50.0
  personalities:
    Kate: "Your ONLY goal on your very first turn is to create a private channel and invite Jack. Use create_group with targets=['Jack'] and a message asking him to join. Do this immediately on turn 1."
    Jack: "If you see a pending invite, always accept it immediately. Accept it before doing anything else."
resources:
  carrying_capacity: 100
  regeneration_factor: 1.5
  fish_per_harvest: 5.0
election:
  method: plurality
  elections_every_round: true
leader:
  fine_destination: common_pool
  default_limit: 10.0
  default_penalty_rate: 0.0
  candidacy_cost: 2.0
llm:
  provider: deepseek
  model: deepseek-chat
  temperature: 0.3
  max_tokens: 500

[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    integration: marks integration tests (requires stub or full engine)
    slow: marks slow tests
    llm: marks tests that require a real LLM API key
